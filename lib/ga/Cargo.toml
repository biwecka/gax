[package]
name = "ga"
version = "0.0.0"
edition = "2021"

[lib]
path = "lib.rs"

[features]
# default = []
# default = ["cache"]
default = ["cache", "rerun-log"]
# default = ["rerun-log"]

cache = []
rerun-log = []

[dependencies]
#> lib
xhstt = { workspace = true }

#> remote
rand = { workspace = true }
rayon = { workspace = true }
ndarray = { workspace = true }
hashbrown = { workspace = true }
rerun = { workspace = true }

[lints]
workspace = true
